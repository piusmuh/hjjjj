#include <stdio.h>  //for input output
#include <stdbool.h> //for the bool

int main(){
  int age;
  

  /*int age = 25;
  int year = 2025;
  float gpa = 3.7;
  char grade = 'A';
  char name[] = "Pius";
  double pi = 3.14677899987;
  bool isOnline = true;
  prinff("%d\n", isOnline);
  printf("You are %d years old in %d\n", age, year );
  printf("Your gpa in %.1f and pi is %.10lf\n", gpa, pi);
  printtf("Hello %s, your grade in %c\n ", name, grade );*/

  printf("Enter your age: ");
  scanf("%d", &age);
  





  return 0;
}